{
  "name": "e2e-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --env allure=true",
    "report": "npx allure generate allure-results --clean -o allure-report",
    "build": "echo 'build script executed'",
    "cy:report": "npm run cleanup && npm run cy_dashboard && npm merge_reports &npm run mochawesome_report"
  },
  "keywords": [],
  "author": "Gireesha Jeera",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.35.2",
    "allure-commandline": "2.17.2",
    "cypress": "^10.11.0",
    "webpack": "^5.75.0"
  },
  "env": {
    "allureResultsPath": "allure-results"
  },
  "dependencies": {
    "webpack": "^5.75.0"
  }
}
